import 'package:flutter/material.dart';

class DitailPage extends StatelessWidget {
  static const String id = 'Ditailpage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.red,height: 100,),
    );
  }
}
